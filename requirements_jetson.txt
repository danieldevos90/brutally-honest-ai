# Brutally Honest AI - Jetson Optimized Requirements
# Optimized for NVIDIA Jetson (ARM64) architecture

# Core FastAPI and async support
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0

# Audio processing (use smaller models on Jetson)
openai-whisper>=20231117
# Note: Use 'base' or 'tiny' models instead of 'medium' or 'large'
pyannote.audio==3.1.1
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
scipy>=1.14.1

# PyTorch for Jetson (install separately if needed)
# For Jetson, use: pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu118
# Or use Jetson-specific wheels from NVIDIA
torch>=2.1.0
torchaudio>=2.1.0

# Speaker diarization (may need to compile some packages)
speechbrain==0.5.16
resemblyzer>=0.1.3

# Local LLM integration
ollama==0.1.7
llama-cpp-python==0.2.11
transformers==4.35.2
requests==2.31.0

# Database and vector search
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
qdrant-client>=1.7.1
alembic==1.12.1
sentence-transformers==2.2.2

# Document processing
PyPDF2==3.0.1
pdfplumber==0.10.0
python-docx==0.8.11
docx2txt==0.8

# USB/Serial communication
pyserial==3.5
pyusb==1.2.1
libusb1==3.0.0

# Security and authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Utilities
numpy>=1.24.3
pandas==2.0.3
python-dateutil==2.8.2

# Jetson-specific optimizations
# Some packages may need to be compiled from source
# If installation fails, try: pip install --no-cache-dir <package>

